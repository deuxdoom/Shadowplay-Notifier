# 변경 내역

이 파일의 형식은 [Keep a Changelog 1.1.0](https://keepachangelog.com/ko/1.1.0/)을
따르며, 버전은 [유의적 버전 2.0.0](https://semver.org/lang/ko/)을 따릅니다.

## [2.1.0] - 2026-09-13

녹화 감시 화면을 월페이퍼와 같은 모습으로 다시 만들었습니다. 이제 녹화가
시작되어도 화면이 통째로 바뀌지 않고 그 위의 내용만 갈립니다. 월페이퍼 쪽도
오래 띄워 두고 눈에 걸리던 곳을 다듬었습니다.

### 새 기능

- **오늘 달의 모양을 보여 줍니다.** 날씨 아래 최고·최저 기온 왼쪽에 지금
  달이 뜹니다. 실제 달 사진에 빛이 닿지 않는 면을 계산해 덮는 방식이라
  표면의 바다와 충돌구가 그대로 보이고, 초승달인지 보름달인지 한눈에
  알아볼 수 있습니다. 그늘진 쪽도 완전히 지우지 않고 어렴풋이 남겨 두어
  달의 윤곽과 차오른 정도를 함께 볼 수 있습니다.
- **날씨 아이콘에 색이 들어갑니다.** 해는 노랗게, 구름은 밝은 회색으로, 비는
  푸르게, 눈은 희게 칠해 멀리서도 무슨 날씨인지 구별됩니다.

### 변경

- **녹화 감시 화면을 다시 만들었습니다.** 월페이퍼와 같은 배경 위에 녹화
  정보를 올립니다. 시계가 있던 자리에는 경과 시간이, 기온이 있던 자리에는
  용량과 비트레이트가, 음악이 있던 자리에는 이벤트 로그가 들어갑니다. 하늘은
  녹화 중에도 시간대와 날씨를 그대로 따라가므로, 녹화가 시작되어도 다른 앱으로
  넘어간 것처럼 보이지 않습니다. 글꼴도 월페이퍼와 같은 것을 씁니다.
- **녹화 중이라는 표시는 절대 흐려지지 않습니다.** 붉은 점과 REC 표시는
  어두워지는 대신 더 밝은 쪽으로만 깜박이고 크기가 함께 커집니다. 어느 순간에
  보아도 녹화가 돌고 있다는 것이 분명합니다.
- **`monitor.log` 에는 녹화와 오류만 남습니다.** 글꼴을 등록한 일이나 곡이
  바뀔 때마다 적히던 줄, 날씨를 받아 온 기록은 빼서 정작 중요한 녹화 기록이
  묻히지 않게 했습니다. 문제를 찾아야 할 때는 `config.json` 에
  `"verbose_log": true` 를 넣으면 예전처럼 자세히 남습니다.
- **시계를 더 크게 키웠습니다.** 시계 아래에 있던 작은 영문 표시와 점은
  없앴습니다. 잘 보이지도 않고 자리만 차지했습니다. 시간대 이름은 시계 위,
  날짜 앞줄로 옮겨 크게 적습니다.
- **날짜와 시계 사이의 구분선을 없앴습니다.** 선이 차지하던 자리는 시계가
  가져갔습니다.
- 오른쪽 날씨도 커진 시계에 맞추어 위치와 크기를 다시 잡았습니다.
- **날씨는 한 시간에 한 번 받아 옵니다.** 기온과 하늘 상태가 그보다 자주
  바뀌지 않아서, 자주 물어보지 않도록 했습니다.
- **곡 정보를 "아티스트 - 곡 이름" 한 줄로 묶고 그 아래에 앨범을 적습니다.**
  세 줄로 나뉘어 오른쪽이 비던 것을 줄이고 글자를 모두 키웠습니다.

## [2.0.0] - 2026-09-13

녹화를 기다리는 동안 화면이 비어 있던 자리를, 시계와 날씨와 지금 듣는 음악을
보여 주는 월페이퍼 화면으로 채웠습니다. 녹화가 시작되면 곧바로 녹화 화면으로
바뀌고, 끝나면 다시 월페이퍼로 돌아옵니다. 늘 켜 두는 보조 디스플레이를 위한
변화입니다.

### 새 기능

- **월페이퍼 화면을 넣었습니다.** 녹화를 기다리는 동안 큰 시계와 날짜, 요일,
  날씨, 그리고 재생 중인 곡을 보여 줍니다. 녹화가 잡히면 곧바로 예전과 같은
  녹화 화면으로 바뀌고, 녹화가 끝나면 다시 이 화면으로 돌아옵니다.
  `config.json` 의 `wallpaper` 를 `false` 로 두면 예전처럼 대기 화면만 나옵니다.
- **음악에 맞춰 움직이는 섬세한 파형**을 지평선 아래에 놓았습니다.
  스피커로 나가는 소리를 받아 40 Hz 부터 16 kHz 까지를 12개 대역으로 나누어
  64개의 가는 막대와 옅은 반사로 그립니다. 대역마다 최근에 나온 소리의 크기를
  따라가므로 곡이 바뀌거나 볼륨을 조절해도 막대가 한쪽에 몰리지 않습니다.
  소리를 녹음하거나 저장하지 않으며,
  세기만 재어 그림으로 옮깁니다.
- **재생 중인 곡과 앨범 커버**를 보여 줍니다. Spotify 창 제목에서 아티스트와
  곡 이름을 읽고, 앨범 커버는 iTunes 의 공개 검색으로 받아 옵니다. 로그인이나
  계정 연결이 필요 없고, Spotify 창이 트레이에 내려가 있어도 곡이 바뀌는 것을
  알아챕니다. 커버는 한 장만 들고 있다가 곡이 바뀌면 갈아 끼우며 저장하지
  않습니다.
- **커버 색이 배경에 번집니다.** 앨범 커버에서 가장 잘 어울리는 색을 뽑아
  지평선 부근을 물들이므로, 곡이 바뀌면 화면 전체의 분위기도 부드럽게 바뀝니다.
  커버 모서리는 둥글게 다듬었으며 음악이 없을 때는 레코드 문양과 ‘고요한 순간’
  화면을 보여 줍니다.
- **시간대와 날씨에 따라 하늘이 달라집니다.** 심야·여명·아침·한낮·해질녘·밤
  여섯 단계 사이를 끊기지 않게 섞어 하루 동안 천천히 물들어 갑니다. 맑으면
  별이 뜨고, 흐리면 별이 가려지며, 구름과 안개가 지평선을 감쌉니다. 비 오는
  날의 물방울은 창문을 타고 내려오고, 눈송이와 맑은 밤의 유성은 잠깐씩 나타났다
  사라집니다. 늘 켜 두는 화면이라 한낮에도 눈이 부시지 않도록 밝기는 어둡게
  묶어 두고 색조만 옮겼습니다.
- **큰 시계와 날씨를 중심으로 화면을 구성했습니다.** 960×640 보조 화면에서
  쉽게 읽히도록 날짜·요일, 기온과 날씨 아이콘을 크게 표시합니다. 날씨 아이콘은
  천천히 움직이며 짧은 비·눈·별 효과가 더해집니다.
- **트레이 기능을 넣었습니다.** 오른쪽 위 X는 항상 보이며 누르면 트레이로
  이동합니다. 트레이를 클릭하면 창이 돌아오고, 우클릭 메뉴에서 월페이퍼 화면,
  녹화 감시 화면, 녹화에 따른 자동 전환을 선택할 수 있습니다. 환경설정,
  GitHub 페이지, 프로그램 종료 메뉴도 제공합니다. 창이 숨어 있는 동안에는
  월페이퍼 렌더링과 음악·날씨 조회를 쉬고 녹화 감시는 계속합니다.
- **날씨를 보여 줍니다.** 지금 기온과 날씨 아이콘, 오늘 최고·최저 기온, 습도,
  강수 확률이 나옵니다. Open-Meteo 를 쓰기 때문에 API 키를 발급받을 필요가
  없습니다. 기본 위치는 서울이며 `config.json` 의 `latitude` 와 `longitude`
  로 바꿀 수 있습니다.
- **한글과 일본어를 함께 담은 글꼴을 넣었습니다.** 일본 노래 제목이 네모 상자로
  깨지거나 어울리지 않는 글꼴로 나오지 않습니다. 글꼴은 이 프로그램이 돌아가는
  동안에만 등록되며 윈도우에 설치되지 않습니다.

### 변경

- 실행 파일·트레이·환경설정 창의 아이콘을 시계와 햇빛, 지평선을 담은 디자인으로
  바꾸었습니다.
- X와 일반 창의 Esc는 트레이로 이동합니다. 완전히 종료하려면 트레이 메뉴의
  ‘프로그램 종료’ 또는 Ctrl+Q를 사용합니다.
- 녹화를 기다릴 때 나오던 "대기중" 화면은 월페이퍼를 끄거나 트레이에서
  녹화 감시 화면을 선택하면 나옵니다.
- `config.json` 에 `wallpaper`, `latitude`, `longitude` 항목이 생깁니다.
  예전 설정 파일을 그대로 두어도 기본값으로 채워집니다.

[2.0.0]: https://github.com/deuxdoom/Shadowplay-Notifier/releases/tag/2.0.0

--- 

## [1.0.1] - 2026-09-08

### 변경

- 실행 파일의 아이콘을 새로운 디자인으로 바꾸었습니다.
- 환경설정 창의 타이틀바에도 같은 아이콘이 나옵니다. 작업 표시줄에서 창을 고를
  때 어느 프로그램의 창인지 한눈에 알아볼 수 있습니다.

### 수정

- 다른 해상도의 모니터에서 전체화면으로 전환하면 녹화 정보는 작게 남고 로그와
  빈 자리만 커지던 문제를 고쳤습니다. 960x640 화면을 기준으로 배너, 녹화 정보,
  로그, 글자와 아이콘을 함께 확대해서 화면 구성의 비율을 그대로 지킵니다.

[1.0.1]: https://github.com/deuxdoom/Shadowplay-Notifier/releases/tag/1.0.1

--- 

## [1.0.0] - 2026-09-07

ShadowPlay 로 게임을 녹화할 때, 지금 녹화가 돌아가고 있는지를 
보조 디스플레이에 상시 표시해 주는 앱입니다. 
NVIDIA App 의 녹화 상태 표시 아이콘이 간헐적으로 영상에 그대로 녹화되는 문제 때문에 
결국 꺼두게 되었는데, 그러면 녹화가 시작되는지 중단되는지 알 길이 없어집니다.
이 앱은 녹화 영상이 기록되는 폴더를 지켜보다가, 녹화가 시작되고 끝나는 것을
보조 모니터에 보여주는 것을 목적으로 만들어 졌습니다.

### 새 기능

- 녹화 폴더를 주기적으로 훑어 파일이 커지는 것을 보고 녹화 여부를 판정합니다.
  한 주기 전보다 파일이 `min_growth` 이상 커지면 녹화 시작으로 보고, 증가가
  `stall` 초 동안 없으면 녹화가 끝난 것으로 봅니다. 임시 파일이 사라지거나
  완료본 폴더에 새 파일이 생기는 경우에도 중단으로 판정합니다. 녹화 프로그램에
  끼어들지 않고 폴더를 읽기만 하므로, 어떤 녹화 도구를 쓰든 상관없이 동작하고
  녹화 결과물에도 손대지 않습니다.
- 파일이 쓰기로 열려 있는지를 함께 보고 시작과 중단을 곧바로 잡아냅니다
  (`fast_detect`, 기본 켜짐). 녹화하는 프로그램은 기록하는 동안 파일을 열어
  두고 멈추는 순간 닫으므로, 이 신호를 쓰면 `stall` 초를 기다리지 않고 폴링
  주기 한 번 안에 중단이 화면에 반영됩니다. 새로 나타난 파일이 쓰기로 열려
  있으면 크기가 한 번 더 늘기를 기다리지 않고 곧바로 시작으로 판정합니다.
  읽기 권한만 요구하고 공유는 읽기까지만 허용해서 여는 방식이라 녹화 결과물을
  고치지도 잠그지도 않으며, 크기가 늘지 않은 주기에만 확인합니다. 판단이 서지
  않는 환경에서는 위의 크기 증가와 `stall` 판정으로 되돌아갑니다.
- 960x640 고정 크기의 화면에 상태 배너, 녹화 정보, 이벤트 로그, 상태 표시줄을
  보여 줍니다. 녹화 중에는 배너가 빨갛게 바뀌고 원형 인디케이터가 1초 주기로
  밝기 펄스를 그려서, 멀리서도 녹화 여부를 한눈에 알아볼 수 있습니다.
- 녹화 정보로 저장 폴더, 파일 이름, 경과 시간, 용량, 비트레이트, 마지막으로
  파일이 커진 시점을 보여 줍니다. 마지막 증가가 늦어지면 색이 주황색에서
  빨간색으로 바뀌므로 녹화가 멎는 낌새를 미리 알 수 있습니다. 대기 중에는 직전
  녹화 요약과 세션 누적 횟수를 대신 표시합니다.
- 작은 보조 화면에서 멀리 떨어져 보는 용도이므로 글자를 크게 잡았습니다. 로그는
  5줄만 두고 남은 자리를 녹화 정보에 몰아주었으며, 저장 폴더는 42pt, 경과 시간과
  용량은 38pt 로 표시합니다. 저장 폴더 이름이 길면 잘라내지 않고 글자 크기를
  42pt 에서 24pt 까지 줄여서 끝까지 보여 줍니다.
- 창을 놓을 모니터를 자동으로 고릅니다. 해상도가 창과 같은 960x640 모니터가
  있으면 그 화면을, 없으면 주 모니터 한가운데를 씁니다. 번호를 직접 지정할 수도
  있습니다.
- 타이틀바 없이 쓰는 것을 기본으로 삼았습니다. 창은 어느 지점을 잡아도 끌리며,
  배너 오른쪽 위의 환경설정·전체화면·닫기 버튼으로 다룹니다. 전체화면으로 바꾸면
  창이 올라가 있는 모니터를 가득 채웁니다.
- 한 번에 하나만 실행됩니다. 이미 떠 있는 상태에서 다시 실행하면 새 창을 띄우지
  않고 원래 창을 앞으로 끌어옵니다. 창이 둘 뜨면 같은 폴더를 두 번 감시하면서
  로그와 알림음이 겹치기 때문입니다.
- 환경설정 창에서 녹화 폴더를 직접 고르고 폴링 주기, 중단 판정 시간, 최소 크기,
  모니터 번호, 항상 위, 테두리 없음, 알림음을 바꿀 수 있습니다. 저장하면 곧바로
  새 설정으로 감시를 다시 시작하므로 앱을 껐다 켤 필요가 없습니다. 설정은
  실행 파일 옆의 `config.json` 에 남고, 파일을 직접 고쳐도 됩니다.
- 녹화 시작과 중단에 짧은 알림음을 낼 수 있습니다. 게임을 녹화하는 중에 알림음이
  함께 녹음되면 안 되므로 기본값은 꺼짐입니다.
- 감지한 내용과 오류를 실행 파일 옆의 `monitor.log` 에 남깁니다. 창을 닫은 뒤에도
  언제 무엇을 녹화했는지 되짚어 볼 수 있습니다.
- 창 오른쪽 아래에 버전을 표시합니다. 윈도우 파일 속성 창에서도 버전과 만든
  사람을 확인할 수 있습니다.

[1.0.0]: https://github.com/deuxdoom/Shadowplay-Notifier/releases/tag/1.0.0
